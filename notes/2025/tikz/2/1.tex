\documentclass{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    axis lines=middle,
    xlabel={$x$},
    ylabel={$\sigma(x)$},
    domain=-6:6,
    samples=200,
    ymin=-0.1, ymax=1.1,
    thick,
    grid=both,
    width=10cm, height=6cm,
    scale=0.8
  ]
    \addplot[blue, thick] {1/(1 + exp(-x))};
  \end{axis}
\end{tikzpicture}
\end{document}